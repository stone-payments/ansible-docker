[Unit]
Description=Docker Watchdog
After=network.target
Requires={{ docker_package }}.service

[Service]
Type=simple
EnvironmentFile=/etc/sysconfig/docker_watchdog
ExecStart=/bin/bash {{docker_watchdog_path}}
